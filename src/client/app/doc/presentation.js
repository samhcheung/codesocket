import React, { PropTypes, Component } from 'react'

const DocPresentation = (props) => {
    return (
      <div className='loadingscreen'>
	      <textarea rows="10" cols="50">
	      </textarea>
      </div>
      )
}

export default DocPresentation;